# Claude rules for this repo

## Git workflow
- **Never push directly to the `main` branch.** Always create a feature branch and
  open a pull request targeting `main`. Changes land on `main` only via merged PRs.
