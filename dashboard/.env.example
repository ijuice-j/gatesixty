# Copy to .env.local and fill in. Both are safe to expose to the browser
# (NEXT_PUBLIC_*); Row-Level Security is what protects the data.
NEXT_PUBLIC_SUPABASE_URL=https://<your-project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_xxx
